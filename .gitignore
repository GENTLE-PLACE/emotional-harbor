.claude/
.agents/
skills-lock.json

# Impeccable — рабочие файлы прячем, общие артефакты храним
.impeccable/*
!.impeccable/config.json
!.impeccable/design.json
!.impeccable/critique/
.impeccable/critique/*
!.impeccable/critique/*.md
.vscode/

# --- Внутренние документы: в публичный репозиторий не идут ---
PRODUCT.md
DESIGN.md
CLAUDE.md
schema.sql
wordstat-*.md

# --- Бэкенд: секретная логика, не для публичного репозитория ---
harbor-backend/

# --- Документы: выписки, договоры, сканы. Только локально ---
# Лендинг ничего не публикует в PDF, поэтому закрываем формат целиком:
# так под защиту попадут и будущие документы, о которых легко забыть.
*.pdf